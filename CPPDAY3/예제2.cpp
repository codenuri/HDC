// 각 OS가 제공하는 C기반 함수를 사용해서 개발하면
// 1. OS별로 함수가 다르므로 따로 작성해야 한다.
// 2. C는 함수 기반이므로 항상 복잡하다.

// C++ GUI 클래스 라이브러리를 사용하면
// 1. C 함수 기반보다 편리합니다.
// 2. 하나의 소스로 다양한 OS에 사용가능합니다.

// C++표준 STL 에는 GUI 관련 클래스 라이브러리 없습니다.

// 대표적인 GUI 라이브러리
// => MFC, QT, wxWidget, nana, .... 
// => 구글 "C++ GUI library"

